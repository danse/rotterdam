closed circuits provide control.  empty spaces. polished
surfaces. closed links, threads. even when delicate and flexible. it's
a different kind of control.

such a thread could be called a routine

an anchor space can be a checkpoint in a routine

queues circle threads for shortest time, stacks for longest time
